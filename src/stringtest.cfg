MEMORY {
    LOADADDR:       start = $0805,  size =     2,   file = %O;
    MAIN:           start = $0805,  size = $7000,   file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    MAIN:           load = MAIN,            type = rw;
}
